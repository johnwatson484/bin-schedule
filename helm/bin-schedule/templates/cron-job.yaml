{{- include "helm-library.cron-job" (list . "bin-schedule.cron-job-fixtures") -}}
{{- define "bin-schedule.cron-job-fixtures" -}}
metadata:
  name: {{.Values.name }}
spec:
  successfulJobsHistoryLimit: 1
  jobTemplate: 
    spec:
      template:
        spec:
          containers:
          - {{ include "helm-library.container" (list . "bin-schedule.container") | nindent 12 }}
{{- end -}}
