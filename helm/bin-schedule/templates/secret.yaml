{{- include "helm-library.secret" (list . "live-scores-publisher.secret") -}}
{{- define "live-scores-publisher.secret" -}}
stringData:
  ADDRESS: {{ .Values.container.address }}
  POSTCODE: {{ .Values.container.postcode }}
  EMAILS: {{ .Values.container.emails }}
  SMTP_HOST: {{ .Values.container.smtpHost }}
  SMTP_USER: {{ .Values.container.smtpUser }}
  SMTP_PASSWORD: {{ .Values.container.smtpPassword }}
{{- end -}}
